import 'package:flutter/material.dart';

class NftEditorPage extends StatelessWidget {
  const NftEditorPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}